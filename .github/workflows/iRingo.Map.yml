name: Update iRingo.Map.sgmodule

on:
  workflow_dispatch: # 手动触发
  schedule:
    - cron: '0 0 * * *' # 每天运行一次

jobs:
  process-file:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Download GeoServices file
      run: |
        curl -L https://github.com/NSRingo/GeoServices/releases/latest/download/Maps.sgmodule -o Module/Tool/iRingo.Maps.sgmodule

    - name: Process file
      run: |
        # 替换 category 为 Tool
        sed -i 's/#!category[[:space:]]*=[[:space:]]*.*$/#!category=Tool/' Module/Tool/iRingo.Maps.sgmodule
        # 删除 [Rule] 部分
        sed -i '/\[Rule\]/,/^$/d' Module/Tool/iRingo.Maps.sgmodule

    - name: Commit and push changes
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add Module/Tool/iRingo.Maps.sgmodule
        git diff-index --quiet HEAD || git commit -m "Update iRingo.Maps.sgmodule"
        git push
